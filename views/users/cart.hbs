<div class="breadcrumb-area bg-gray menuBarTop">
    <div class="container">
        <div class="breadcrumb-content text-center">
            <ul>
                <li>
                    <a href="/viewAll">Home</a>
                </li>
                <li class="active">Cart Page </li>
            </ul>
        </div>
    </div>
</div>

{{#if cartData}}
<div class="cart-main-area pt-50 pb-120 ">
    <div class="container">
        <h3 class="cart-page-title">Your cart items </h3>
        <div class="row">
            <div class="col-lg-12 col-md-12 col-sm-12 col-12">
                <div class="table-content table-responsive cart-table-content d-none d-md-block">
                    <table>
                        <thead>
                            <tr>
                                <th>Image</th>
                                <th>Product Name</th>
                                <th>Util Price</th>
                                <th>Qty</th>
                                <th>Subtotal</th>
                                <th>action</th>
                            </tr>
                        </thead>
                                <form action="/proceed-checkout" method="post">

                        <tbody>
                            {{#each cartData}}
                            <tr>

                                <td class="product-thumbnail">
                                    <a href="/product-details/{{_id}}"><img
                                            src="User Assets/images/product/{{image.[0]}}" alt="" width="100"></a>
                                </td>
                                <td class="product-name"><a
                                        href="/product-details/{{_id}}">{{productname}}</a></td>
                                <td class="product-price-cart"><span class="amount">Rs.
                                        {{price}}</span>
                                </td>
                                <td class="product-quantity pro-details-quality">
                              
                                       <div>
                                        <a class="btn btn-light" style="display: inline-block;"
                                             onclick="this.parentNode.querySelector('input[type=number]').stepDown();validateData('{{_id}}')">-</a>
                                             <span style="display: inline-block; width: 40px;"><input id="{{_id}}" name="quantity" min="1" max="{{quantity}}" value="1"
                                                    pattern="{{_id}}" type="number" onchange="validateData('{{_id}}')"></span>
                                       {{!-- <span><input id="{{_id}}" name="quantity" min="1" max="{{quantity}}" value="1"
                                                    pattern="{{_id}}" type="number" onchange="validateData('{{_id}}')"></span> --}}
                                        <a class="btn btn-light" style="display: inline-block;"
                                             onclick="this.parentNode.querySelector('input[type=number]').stepUp();validateData('{{_id}}')">+</a>
                                        
                                    </div>
                                           
                                                
           

                  {{!-- hidden --}}
                  {{!-- <input type="hidden" name="count" value="{{this.count}}"> --}}

                
                                   
                                    
                                </td>
                                {{!-- <td class="product-subtotal"> Rs.
                                    <span id="{{this.product.offerPrice}}">{{subtotal}}</span>
                                </td> --}}

                                <td class="product_total">
                                         <span>  
                                                <p id="actual_price_{{_id}}" hidden="">Rs.{{price}}</p>
                                                <span id="price_text_{{_id}}">Rs.{{price}}</span>
                                         </span>
                                </td>
                                {{!-- <td class="product-remove">

                                    <button class="btn btn-outline-danger"
                                        onclick="removeCartProduct('{{_id}}')">X</button>

                                </td> --}}
                        <td class="product_remove">
                            <a class="btn btn-link px-0 text-danger" type="reset" onclick="removeCartProductss('{{_id}}')">
                                <i class="ci-close-circle me-2"></i>
                                <span class="fs-sm">X</span>
                            </a>
                        </td>

                            </tr>
                            {{/each}}

                        </tbody>
                    </table>

                </div>

                {{!-- table for mobile --}}
                <div class=" d-block d-md-none">
                    {{#each products}}
                    <div class="" style="height: 200px; ">
                        <div class=" col-4  float-left">
                            <a href="#"><img src="/images/product-img/{{this.product.image1}}" width="100" alt=""></a>
                        </div>
                        <div class="col-8  float-right">
                            <a class="font-weight-bold" href="#">{{this.product.title}} </a>
                            <p>MRP:{{this.product.MRP}} {{this.product.totalDiscount}}% Off <br>Offer Price:
                                {{this.product.offerPrice}}</p>
                            <div class="mt-3">
                                <div>
                                    <button class="btn btn-light" style="display: inline-block;"
                                        onclick="changeQuantity('{{this._id}}','{{this.product._id}}','{{../userId}}','{{this.product.offerPrice}}',-1)">-</button>
                                    <span style="display: inline-block; width: 20px; "
                                        id="{{this.product._id}}{{this.product._id}}">{{this.quantity}}</span>
                                    <button class="btn btn-light" style="display: inline-block;"
                                        onclick="changeQuantity('{{this._id}}','{{this.product._id}}','{{../userId}}','{{this.product.offerPrice}}',1)">+</button>
                                        <span id="{{this.product.offerPrice}}{{this.product.offerPrice}}">{{this.productTotal}}</span> 
                                </div>
                                  
                                <button onclick="removeCart('{{this._id}}','{{this.product._id}}')"
                                    class="btn btn-outline-danger btn-sm ml-3"><i class="bi bi-trash3"></i></button>
                            </div>
                        </div>
                    </div>
                    {{/each }}
                </div>

                <div class="row">
                    <div class="col-lg-6 col-md-12">
                        <div class="grand-totall">
                            <div class="title-wrap">
                                <h4 class="cart-bottom-title section-bg-gary-cart">Cart Total</h4>
                            </div>
                            <h5>Total price <span><i></i>Rs.<span
                                        id="total">{{subtotal}}</span> </span></h5>
                            <div class="total-shipping">
                                <h5>Total shipping</h5>
                                <ul>
                                    <li><input type="checkbox"> Standard <span>Free </span></li>
                                    
                                </ul>
                            </div>
                            <h4 class="grand-totall-title">Grand Total <span><i></i>Rs.<span
                                        id="subTotal">{{subtotal}}</span> </span></h4>
                            {{!-- <a href="/checkout">Proceed to Checkout</a> --}}

                                
                             <button style="width: 100%;height:50px;background-color:#fc2779;font-size:medium;color:white;border-style:none" type="submit">
                                Proceed to Checkout
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{{else}}
<div class="cart-main-area pt-115 pb-120">
    <div class="container">
        <h3 class="cart-page-title">Your cart is Empty Please Add items to continue</h3>
        <div class="row ">
            <div class="col-lg-12 col-md-12 col-sm-12 col-12">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="cart-shiping-update-wrapper">
                            <div class="cart-shiping-update">
                                <a style="border: 1px rgb(125, 125, 125);border-style:solid" href="/viewAll">Continue Shopping</a>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
{{/if}}

<script>

    {{!-- function removeCartProduct(cartId) {
        alert(123)
        Swal.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                removeCart(cartId)

            }
        })
    }  --}}


   async function removeCartProductss(itemid){
        let response = await fetch('/cart-delete/' + itemid, {
            method: 'DELETE', headers: {
                'Content-Type': 'application/json',
            },
        });

        let res = await response.json();
        if (res == "Success") {
            {
                Swal.fire({
                    title: 'Success',
                    text: "Item deleted successfully !",
                    icon: 'success',
                    confirmButtonColor: '#3085d6',
                    cancelButtonColor: '#d33',
                    confirmButtonText: 'OK',
                    timer: 3000
                }).then((res) => {
                    window.location.reload()
                })
            }
        } else
            try {
                console.log("Hello")
            }
            catch (error) {
                console.log(error.message)
            }
     }

    //quantity
     function validateData(id) {
        const element = document.getElementById(id)
        const priceText = document.getElementById('price_text_' + id)
        const price = document.getElementById('actual_price_' + id)
        const total = document.getElementById('total')
        const subTotal=  document.getElementById('subTotal')
        if (Number(element.value) > Number(element.max)) element.value = element.max
        let cost = priceText.innerHTML
        cost = cost.split(".")
        const rate = cost[1]
        let p = price.innerHTML
        p = p.split(".")
        if(p.length) p = Number(p[1]) * Number(element.value)
        priceText.innerHTML = 'Rs. ' + p
        let totalPriceCart = priceText.innerHTML
        totalPriceCart = totalPriceCart.split(".")
        const diff = Number(totalPriceCart[1]) - Number(rate)
        total.innerHTML = Number(diff) + Number(total.innerHTML)
        subTotal.innerHTML = Number(diff) + Number(subTotal.innerHTML)

     }

      async function proceedTocheckout() {
        const price = Object.values(document.getElementById('price_text_' + id)).map((item)=>({
            price:item.value
        }))
        const quantity = Object.values(document.getElementsByName("quantity")).map((item) => (
            {
               
                quantity: item.value
            }
        ))
        let response = await fetch('/proceed-checkout', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', },
            body: JSON.stringify(quantity)
        });
        let result = await response.json()
    }

</script>